# Example environment variables
PORT=8080
GOOGLE_PROJECT=your-google-project
FASTMCP_API_KEY=replace-me
# Optional: GitHub token to increase rate limits (DO NOT commit secrets)
GITHUB_TOKEN=
# Gemini endpoint and API key (if using a hosted Gemini inference endpoint)
GEMINI_API_URL=
GEMINI_API_KEY=
